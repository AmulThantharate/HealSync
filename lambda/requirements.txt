# boto3 is pre-installed in the Lambda Python 3.12 runtime — do not bundle it.
# All other dependencies are Python stdlib only (urllib, json, os, time).
# This file intentionally left empty for Lambda packaging.
